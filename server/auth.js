// if (process.env.USER && process.env.PASS){
//     var basicAuth = new HttpBasicAuth(process.env.USER, process.env.PASS);
//     basicAuth.protect();
// }
// else {
//     throw("server/auth.env doesn't exist");
// }
